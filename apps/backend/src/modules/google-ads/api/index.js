const googleAdsClient = require('./client');
const { getCustomer } = require('./customer');
const { runQuery } = require('./report');

module.exports = {
  googleAdsClient,
  getCustomer,
  runQuery,
}; 